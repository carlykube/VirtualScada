The \doxyref{Illuminate}{p.}{namespace_illuminate} Database component is a full database toolkit for \doxyref{P\+H\+P}{p.}{namespace_p_h_p}, providing an expressive query builder, Active\+Record style O\+R\+M, and schema builder. It currently supports My\+S\+Q\+L, Postgres, S\+Q\+L Server, and S\+Q\+Lite. It also serves as the database layer of the \doxyref{Laravel}{p.}{namespace_laravel} \doxyref{P\+H\+P}{p.}{namespace_p_h_p} framework.

\subsubsection*{Usage Instructions}

First, create a new \char`\"{}\+Capsule\char`\"{} manager instance. Capsule aims to make configuring the library for usage outside of the \doxyref{Laravel}{p.}{namespace_laravel} framework as easy as possible.

```\+P\+H\+P use \doxyref{Illuminate}{p.}{namespace_illuminate} as Capsule;

\$capsule = new Capsule;

\$capsule-\/$>$add\+Connection([ \textquotesingle{}driver\textquotesingle{} =$>$ \textquotesingle{}mysql\textquotesingle{}, \textquotesingle{}host\textquotesingle{} =$>$ \textquotesingle{}localhost\textquotesingle{}, \textquotesingle{}database\textquotesingle{} =$>$ \textquotesingle{}database\textquotesingle{}, \textquotesingle{}username\textquotesingle{} =$>$ \textquotesingle{}root\textquotesingle{}, \textquotesingle{}password\textquotesingle{} =$>$ \textquotesingle{}password\textquotesingle{}, \textquotesingle{}charset\textquotesingle{} =$>$ \textquotesingle{}utf8\textquotesingle{}, \textquotesingle{}collation\textquotesingle{} =$>$ \textquotesingle{}utf8\+\_\+unicode\+\_\+ci\textquotesingle{}, \textquotesingle{}prefix\textquotesingle{} =$>$ \textquotesingle{}\textquotesingle{}, ]);

// Set the event dispatcher used by \doxyref{Eloquent}{p.}{class_eloquent} models... (optional) use \doxyref{Illuminate}{p.}{namespace_illuminate}; use \doxyref{Illuminate}{p.}{namespace_illuminate}; \$capsule-\/$>$set\+Event\+Dispatcher(new Dispatcher(new Container));

// Make this Capsule instance available globally via static methods... (optional) \$capsule-\/$>$\doxyref{set\+As\+Global()}{p.}{namespace_illuminate_1_1_support_1_1_traits_a93d494bed4892616de88f21fa6e271cc};

// Setup the \doxyref{Eloquent}{p.}{class_eloquent} O\+R\+M... (optional; unless you\textquotesingle{}ve used set\+Event\+Dispatcher()) \$capsule-\/$>$boot\+Eloquent(); ```

\begin{quote}
{\ttfamily composer require \char`\"{}illuminate/events=5.\+0.$\ast$\char`\"{}} required when you need to use observers with \doxyref{Eloquent}{p.}{class_eloquent}. \end{quote}


Once the Capsule instance has been registered. You may use it like so\+:

{\bfseries Using The Query Builder}

```\+P\+H\+P \$users = Capsule\+::table(\textquotesingle{}users\textquotesingle{})-\/$>$where(\textquotesingle{}votes\textquotesingle{}, \textquotesingle{}$>$\textquotesingle{}, 100)-\/$>$get(); ``` \doxyref{Other}{p.}{namespace_other} core methods may be accessed directly from the Capsule in the same manner as from the \doxyref{D\+B}{p.}{class_d_b} facade\+: ```\+P\+H\+P \$results = Capsule\+::select(\textquotesingle{}select $\ast$ from users where id = ?\textquotesingle{}, array(1)); ```

{\bfseries Using The \doxyref{Schema}{p.}{class_schema} Builder}

```\+P\+H\+P Capsule\+::schema()-\/$>$create(\textquotesingle{}users\textquotesingle{}, function(\$table) \{ \$table-\/$>$increments(\textquotesingle{}id\textquotesingle{}); \$table-\/$>$string(\textquotesingle{}email\textquotesingle{})-\/$>$unique(); \$table-\/$>$timestamps(); \}); ```

{\bfseries Using The \doxyref{Eloquent}{p.}{class_eloquent} O\+R\+M}

```\+P\+H\+P class User extends \doxyref{Illuminate}{p.}{namespace_illuminate} \{\}

\$users = User\+::where(\textquotesingle{}votes\textquotesingle{}, \textquotesingle{}$>$\textquotesingle{}, 1)-\/$>$get(); ```

For further documentation on using the various database facilities this library provides, consult the {\tt Laravel framework documentation}. 