The \doxyref{Symfony}{p.}{namespace_symfony} Event\+Dispatcher component implements the Mediator pattern in a simple and effective way to make your projects truly extensible.

```php use \doxyref{Symfony}{p.}{namespace_symfony}; use \doxyref{Symfony}{p.}{namespace_symfony};

\$dispatcher = new Event\+Dispatcher();

\$dispatcher-\/$>$add\+Listener(\textquotesingle{}event\+\_\+name\textquotesingle{}, function (\doxyref{Event}{p.}{class_event} \$event) \{ // ... \});

\$dispatcher-\/$>$dispatch(\textquotesingle{}event\+\_\+name\textquotesingle{}); ```

\subsection*{Resources }

You can run the unit tests with the following command\+: \begin{DoxyVerb}$ cd path/to/Symfony/Component/EventDispatcher/
$ composer install
$ phpunit\end{DoxyVerb}
 